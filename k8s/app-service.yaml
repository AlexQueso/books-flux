apiVersion: v1
kind: Service
metadata:
  name: books
  labels:
    name: books
  namespace: books
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8881
  selector:
    name: books
  type: NodePort
